# Changelog

本工具仍在持续演进中，以下为各阶段的变更记录。
格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

- 接入应用日志系统：运行时信息/错误写入 `data/logs/`，便于排查问题。
- 安全修复：`overall_stats` 的 SQL 移除字符串插值拼接，改为参数化查询，消除潜在注入风险（经 ai-sec-scan 扫描确认无真实高危问题）。
- 模拟考及格线对齐实际标准：改为總錯 ≤8（正確率≥84%）且每冊錯 ≤2，新增分冊錯題統計與超冊提示。

## [0.4.0] - 2026-08-16

### 新增
- `learn` 命令：浏览学习，正确答案绿色背景高亮，`Enter` 下一题 / `r` 上一题。
- `practice` 命令（取代 `book`）：`practice book <册> [题号]`，可指定题号开始。
- 题号跳转：practice / learn 支持 `book N 题号` 从指定题开始。
- 放大图片本地缓存（`data/images_cache/`），二次显示免重复处理。
- 图片强行放大：用 ImageMagick convert 插值放大小源图，保证较大显示仍清晰。

### 变更
- 命令仅保留英文，移除全部中文别名（`exit` → `quit` 等）。
- 图片宽度与题目区对齐、固定每张大小。
- `stats` 输出改为不清屏、直接显示统计表。

### 修复
- 底部横线在选中最下选项时缺失（图片高度自适应终端）。
- 选题部分紧贴图片下方、横线留空缓冲。
- 图片与题目横线间隔加大，避免压线。

## [0.3.0] - 2026-08-16

### 新增
- 全屏单题刷题：上下箭头选择、Right 图片与题目左右并排、答错可重答一次。
- kitty icat 真彩图片内嵌显示。

### 变更
- 图片渲染由 chafa 半块画改为 kitty icat 真彩原图。
- 布局经历多次调整（居中/边框/横线分隔/行距）。

## [0.2.0] - 2026-08-16

### 新增
- 交互式命令界面（MDrivePractice）：命令输入 + fuzzy 下拉补全。
- `start / random / wrong / exam / stats` 各模式。
- 成绩统计（整体 + 分章节）。

## [0.1.0] - 2026-08-16

### 新增
- 初始版本：五册 647 题题库 + 242 幅图片。
- 三层架构：scraper（抓取）/ database（SQLite 存储）/ trainer（练习）。
- 顺序 / 随机 / 章节 / 错题模式。

[Unreleased]: #
