# 駕考題庫練習工具 — 架構文件（Blueprint）

> 狀態：核心功能已完整實現（2026-08）
> 定位：本地、終端內執行的駕考筆試題庫練習工具。題庫資料隨專案內建，全程離線。

## 1. 專案定位

解決在網頁上刷駕考筆試不高效的問題：把題庫放到本地 SQLite，用一個互動式終端程式來練。

- 內建題庫：五冊共 647 題，按冊分類，含 242 幅題目圖片。
- 個人使用、本地離線、不對外提供服務。
- 目標終端：kitty（題目圖片以 icat 真彩顯示）。

## 2. 總體架構

```
┌──────────────────────────────────────────────┐
│            兩層解耦                           │
│                                              │
│  [database]  SQLite 題庫 + 練習進度  (資料中樞) │
│  [trainer]   互動式 CLI 刷題多模式   (日常使用) │
└──────────────────────────────────────────────┘
```

- **database**：題庫資料與作答流水的儲存與存取層。
- **trainer**：讀取題庫，提供多種模式；含互動命令介面、全螢幕刷題、日誌。

練習端只讀資料層，兩者透過 SQLite 互動，互不耦合。

### 目錄結構

```
database/   SQLite：schema.sql（表結構）、store.py（資料存取 API）
trainer/    shell.py（互動命令介面 + 兩級補全）
            tui.py（全螢幕單題刷題：圖片/選項/錯題重答/學習模式）
            main.py（練習端核心：抽題/統計/模擬考）
            logger.py（應用日誌，寫入 data/logs/）
doc/        里程碑進度文件（M3~M8）
data/       questions.db（題庫+作答流水）、images/（圖片）、logs/（日誌）
```

## 3. 資料層設計（SQLite）

- `questions` 表：題目（source_id 唯一、章節 subject、題幹 stem、選項 choices(JSON)、答案 answer、解析 explanation）。
- `practice_log` 表：作答流水（題目 id、對錯、模式、時間）。
- 依 `practice_log` 可推算：正確率、錯題本、分章節統計。

## 4. 使用者介面

### 4.1 互動命令介面（`shell.py`）

啟動後清屏顯示標題，底部輸入命令（兩級補全），命令：

| 命令 | 說明 |
|------|------|
| `start [N]` | 順序刷 N 題 |
| `random [--count N]` | 隨機抽 N 題 |
| `wrong [--count N]` | 錯題重練 N 題 |
| `exam [--count N]` | 模擬考（統一交卷批改） |
| `stats` | 成績統計（整體 + 分章節） |
| `practice book <冊> [題號]` | 答題練習某冊，可指定題號開始 |
| `learn book <冊> [題號]` | 學習瀏覽（正確答案綠色高亮） |
| `edata YYYY-MM-DD` | 設考試日期 + 倒數計時（重啟時標題下顯示） |
| `help` / `clear` / `quit` | 幫助 / 重顯標題 / 退出 |

- 退出（`quit` / `Ctrl+C`）時先顯示一次成績統計再道別。
- 啟動時若已設定考試日期，標題下方自動顯示「距 離 考 試 還 有 N 天」。

### 4.2 全螢幕單題刷題（`tui.py`）

- 每題一頁：題目與選項靠左、帶青色橫線分隔、圖片在上方（kitty icat 真彩）。
- `↑`/`↓` 選擇選項，`Enter` 確認，`q` 退出。
- 錯題重答：答錯給一次重答機會，正確答案高亮。
- learn 學習模式：`Enter` 下一題 / `r` 上一題 / `q` 退出。

### 4.3 圖片顯示

- kitty 終端用 `icat` 真彩內嵌顯示；先以 convert 插值放大後顯示較大且清晰。
- 放大結果快取至 `data/images_cache/`，二次顯示免重複處理。
- 圖片寬度與題目區對齊、每張固定大小、不超屏、不與橫線重疊。

## 5. 里程碑

| 里程碑 | 內容 | 狀態 |
|--------|------|------|
| M0 | 環境建置 + 調研 | ✅ |
| M1 | 資料抓取 + SQLite 儲存 | ✅ |
| M2 | 全量抓取 647 題 + 242 圖 | ✅ |
| M3 | 練習端基礎（順序/隨機/章節） | ✅ |
| M4 | 錯題重練 + 成績統計 | ✅ |
| M5 | 模擬考 + kitty 真彩圖片 + README | ✅ |
| M6 | 互動命令介面 + practice/learn + 題號跳轉 | ✅ |
| M7 | 全螢幕單題刷題 + 圖片放大/快取 | ✅ |
| M8 | 日誌 / 倒數計時 / 繁體介面 / 及格線對齊 / 安全修復 / GitHub 完善 | ✅ |

進度記錄：`doc/M3-status.md` ~ `doc/M8-status.md`。

## 6. 模擬考及格標準（對齊實際）

依澳門交通事務局實際標準：**50 題、總錯 ≤ 8（正確率 ≥ 84%）且每冊錯 ≤ 2**。模擬考交卷以雙標準判定，並顯示分冊錯題與超冊提示。

## 7. 已知限制

- 題庫結構基於 SQLite 現有資料；需更新時資料層提供 upsert 介面。
- 圖片題需 kitty 終端才能顯示內嵌圖；其他終端僅提示路徑。
- 圖片原始解析度約 165px，放大後仍受源圖解析度限制。

## 8. 後續可選

- 匯出題庫為 CSV/JSON 備份。
- 錯題重練按錯次加權排序。
- 作答時間維度統計。
- 排行榜 / 多次模擬考成績曲線。

---

*本文檔隨專案推進持續更新。*
