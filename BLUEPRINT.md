# 驾考题库练习工具 — 架构文档（Blueprint）

> 状态：核心功能已实现（2026-08）
> 定位：本地、终端内运行的驾考笔试题库练习工具。题库数据随项目内置，全程离线。

## 1. 项目定位

解决在网页上刷驾考笔试不高效的问题：把题库放到本地 SQLite，用一个交互式终端程序来练。

- 内置题库：五册共 647 题，按册分类，含 242 幅题目图片。
- 个人使用、本地离线、不对外提供服务。

## 2. 总体架构

```
┌──────────────────────────────────────────────┐
│            两层解耦                           │
│                                              │
│  [database]  SQLite 题库 + 练习进度  (数据中枢) │
│  [trainer]   交互式 CLI 刷题多模式   (日常使用) │
└──────────────────────────────────────────────┘
```

- **database**：题库数据与作答流水的存储与访问层。
- **trainer**：读取题库，提供多种练习模式。

练习端只读数据层，两者通过 SQLite 交互，互不耦合。

### 目录结构

```
database/   SQLite：schema.sql（表结构）、store.py（数据访问 API）
trainer/    main.py（交互式 CLI：顺序/随机/错题/统计/模拟考）
doc/        进度文档
data/       运行数据：questions.db（题库+作答流水）、images/（图片）
```

## 3. 数据层设计（SQLite）

- `questions` 表：题目（source_id 唯一、章节 subject、题干 stem、选项 choices(JSON)、答案 answer、解析 explanation）。
- `practice_log` 表：作答流水（题目 id、对错、模式、时间）。
- 依据 `practice_log` 可推算：正确率、错题本、分章节统计。

## 4. 练习端设计（trainer）

交互式终端刷题，用 rich 做界面（彩色面板、表格、对错反馈）。

| 模式 | 说明 |
|------|------|
| 顺序 sequential | 从题库按序刷 |
| 随机 random | 随机抽题 |
| 章节 --subject | 只练指定册 |
| 错题 wrong | 只练答错过的题 |
| 统计 stats | 整体+分章节正确率 |
| 模拟考 exam | 随机抽题、统一批改、85% 及格线 |

- 作答：输入 A/B/C/D，即时反馈；`q` 退出、`s` 跳过（计错）；进度实时入库。
- 图片题：kitty 终端自动真彩内嵌显示；其他终端提示路径或 ASCII。

## 5. 里程碑（全部完成 ✅）

- ✅ 数据层：schema + store API（查询/错题/统计）
- ✅ 练习端基础：顺序/随机/章节
- ✅ 数据能力：错题重练、成绩统计、错题本
- ✅ 模拟考 + 打磨：模拟考模式、进度持久化、kitty 真彩图片显示、README

进度记录：`doc/M3-status.md` ~ `doc/M5-status.md`。

## 6. 数据与使用说明

- 题库数据随项目发布（`data/questions.db` + `data/images/`），clone 后即可直接用。
- 使用命令见 `README.md`。

## 7. 已知限制

- 题库结构基于 SQLite 现有数据；若需更新题库，数据层提供 upsert 接口。
- 图片题在 kitty 终端可真彩内嵌；其他终端需 `--view-image ascii` 或外部查看器。

## 8. 后续可选

- 导出题库为 CSV/JSON 备份。
- 错题重练按错次加权排序。
- 作答时间维度统计。

---

*本文档随项目推进持续更新。*
